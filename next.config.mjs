/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['t3.ftcdn.net','c0.wallpaperflare.com','localhost','res.cloudinary.com']
    }
};

export default nextConfig;
